{
  "version": 2,
  "name": "server-app-hw",
  "builds": [
    { "src": "src/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.ts" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/src/index.ts" }
  ]
}
